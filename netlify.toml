[build]
  publish = "public"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# 정적 사이트라 별도 빌드 커맨드가 필요 없습니다.
# functions 디렉터리의 각 .js 파일은 자동으로
# /.netlify/functions/<파일명> 경로의 API가 됩니다.
